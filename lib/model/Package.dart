import 'package:flutter/material.dart';

class Package
{
   String? name;
   String? details;
   String? price;
   bool? selected;
   IconData? icons;

  Package(this.name, this.details, this.price, this.selected, this.icons);

}